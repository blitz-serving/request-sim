# Why Your LLM Benchmark Is Under-Counting Prefix Cache Hits (and How We Fixed It)

*Multi-turn KV cache consistency in request-sim*

---

When you benchmark an LLM inference engine with multi-turn conversational traces, prefix caching should be giving you a massive performance boost. Turn 2 of a conversation shares the entire context of turn 1 -- that's potentially thousands of tokens the engine doesn't need to recompute.

Except request-sim was throwing those cache hits away. Here's why, and what it took to fix.

## The Problem

request-sim works with production traces from the Bailian platform. Each trace entry records the conversation's token structure as a vector of `hash_ids` -- opaque block identifiers where each block represents 16 tokens. When two requests share the same `hash_id` at the same position, their KV cache blocks should be identical.

The tool generates synthetic text for each `hash_id` block via `TokenSampler`: sample random token IDs, decode to text, frame with `<|im_start|>` and `<|im_end|>` separators, verify the encode-decode roundtrip produces exactly `BLOCK_SIZE` tokens. Same `hash_id` always maps to the same text (memoized in `user_prompts: HashMap<u64, String>`). This ensures block congruence -- the same hash always produces the same tokens at the same position.

The congruence property works perfectly for single-turn requests. The problem appears in multi-turn conversations.

Consider a two-turn conversation where the user asks a question, the model responds, and the user asks a follow-up. In the Bailian trace format:

```
Turn 1: hash_ids = [A, A, A, B, C]           input_length = 80
Turn 2: hash_ids = [A, A, A, B, C, X, X, D]  input_length = 128  
```

Turn 2's `hash_ids` include all of turn 1's blocks, plus new blocks `[X, X, D]`. Those `X` blocks represent the model's output from turn 1 -- the response text that was generated by the engine. Block `D` is the user's follow-up question.

Here's the catch: request-sim generates *random synthetic text* for those `X` blocks, just like any other `hash_id`. But the real engine's KV cache contains the *actual generated output* from turn 1. The synthetic text in the `X` blocks doesn't match, so the prefix cache lookup breaks at the boundary between the shared input blocks and the output blocks.

The result: request-sim's prefix cache hit rate significantly understates the engine's real caching efficiency, producing pessimistic benchmark numbers for any workload with multi-turn conversations.

## Why It's Harder Than It Looks

The obvious fix sounds simple: capture the engine's output from turn 1 and inject it into turn 2's prompt at the right position. Two algebraic barriers make this impossible with the existing architecture.

### Barrier 1: The Template Suffix Wall

In the original architecture, `inflate()` returns a `PromptPayload::Content(text)` -- a raw string. The API layer wraps it in a single user message: `[{"role": "user", "content": text}]`. The inference engine then applies the chat template, producing:

```
<|im_start|>user\n{text}<|im_end|>\n<|im_start|>assistant\n
```

Now consider what happens at the boundary between shared and new content:

```
Turn 1: <|im_start|>user\n[shared_blocks]<|im_end|>\n<|im_start|>assistant\n
                                         ^^^^^^^^^^^
Turn 2: <|im_start|>user\n[shared_blocks][output_blocks][new_blocks]<|im_end|>\n...
                                         ^^^^^^^^^^^^^^
```

At position `|prefix| + |shared_blocks|`:
- Turn 1's cached token sequence has token 151645 (`<|im_end|>`)
- Turn 2's input has the first token of `output_blocks`

**151645 will never equal a content token.** The template suffix creates an impassable barrier. No matter what text we inject into the output blocks -- random noise or the actual engine output -- the engine's cached `<|im_end|>` at that position can never match.

This isn't a bug. It's a fundamental structural constraint: Content mode packs everything into one message, and the template wrapping creates a token at the exact position where multi-turn output should begin.

### Barrier 2: BPE Non-Distributivity

Even if we could somehow bypass the template suffix, there's a second problem. Suppose we capture the engine's output text and split it into `BLOCK_SIZE`-token chunks to store as hash_id entries. We'd need:

```
enc(chunk_1 ++ chunk_2 ++ ... ++ chunk_n) == enc(chunk_1) ++ enc(chunk_2) ++ ... ++ enc(chunk_n)
```

This is the distributivity of `enc` (BPE tokenization) over `app` (string concatenation). And it doesn't hold. BPE merges can cross the concatenation boundary:

```
enc("helloworld") may yield [tok_helloworld]              -- 1 token
enc("hello") ++ enc("world") yields [tok_hello, tok_world] -- 2 tokens
```

When no pre-tokenization split point exists between two adjacent strings, BPE will happily merge tokens across the boundary. The total token count changes. And since request-sim relies on exact token counts to match trace lengths (`|enc(inflate_text)| == input_length`), any cross-boundary merge breaks the length constraint.

request-sim's block system already solves this for *input* blocks: each block is framed with `<|im_start|>` (151644) and `<|im_end|>` (151645), which are **added tokens** in the Qwen2 tokenizer. The pre-tokenizer always splits at their positions, making `enc` distribute over concatenation of framed blocks:

```
enc(framed_b1 ++ framed_b2) = enc(framed_b1) ++ enc(framed_b2)
```

But output text has no such framing. The engine returns plain text through `choices[].delta.content` (streaming) or `choices[].message.content` (non-streaming), with `skip_special_tokens=True`. Splitting this plain text into arbitrary chunks and reassembling is not token-count-preserving.

## The Solution: Messages Architecture

The fix requires a structural change to how multi-turn prompts are represented.

Instead of `PromptPayload::Content(text)` with a single-message wrapper, multi-turn requests use `PromptPayload::Messages(Value)` with the actual role structure:

```json
// Turn 1: single user message
[{"role": "user", "content": "turn 1 text"}]

// Turn 2: user + assistant + user
[
  {"role": "user", "content": "turn 1 text"},
  {"role": "assistant", "content": "actual engine output from turn 1"},
  {"role": "user", "content": "turn 2 new text"}
]
```

The engine applies the chat template, producing:

```
Turn 1: <|im_start|>user\n{turn1_text}<|im_end|>\n<|im_start|>assistant\n
Turn 2: <|im_start|>user\n{turn1_text}<|im_end|>\n<|im_start|>assistant\n{output}<|im_end|>\n<|im_start|>user\n{new_text}<|im_end|>\n<|im_start|>assistant\n
```

Now the prefix match analysis works:

- Turn 2's token sequence starts with Turn 1's *entire* cached sequence, including the `<|im_end|>\n<|im_start|>assistant\n` tokens and the output tokens.
- The prefix match extends through the output region, because `{output}` in Turn 2 is the same text the engine generated in Turn 1.
- The match only breaks at the new content boundary, where Turn 2 adds `<|im_end|>\n<|im_start|>user\n{new_text}...`.

This solves both barriers simultaneously:

1. **Template tokens become allies, not enemies.** The `<|im_end|>\n<|im_start|>assistant\n` sequence that blocked us in Content mode is now *part of the shared prefix*. It sits between the user message and the assistant message in both turns, at exactly the same position.

2. **BPE distributivity is restored at message boundaries.** Chat template tokens (`<|im_start|>`, `<|im_end|>`) are added tokens that act as pre-tokenization barriers. Within each message, content is tokenized as a single contiguous unit -- no arbitrary chunking, no cross-boundary merges. The output text goes into one assistant message and is tokenized in one pass, exactly as the engine originally tokenized it.

### Implementation: Coordinating Across Turns

The `--track-output` flag activates a `TrackOutputState` that coordinates output capture and injection across concurrent requests:

```rust
pub struct TrackOutputState {
    pub graph: Arc<ConversationGraph>,
    pub tokenizer: Arc<tokenizers::Tokenizer>,
    completions: Arc<DashMap<usize, watch::Sender<Option<String>>>>,
}
```

The `ConversationGraph` is built at startup from the trace's `chat_id` / `parent_chat_id` fields. It provides:

- `parent_index[i]`: which data index is the parent of request `i`
- `delta_hashes[i]`: hash IDs in request `i` that don't appear in the parent
- `parent_output_blocks[i]`: `ceil(parent.output_length / BLOCK_SIZE)` -- how many delta blocks represent output

The dispatch loop in `spawn_request_loop_with_timestamp` works as follows:

1. **Before dispatching request `i`**: if `i` has a parent, call `tos.wait_for_ancestors(data_index).await`. This uses `tokio::sync::watch` channels to block until all ancestor turns have completed and stored their output text.

2. **Inflate as Messages**: call `inflate_as_messages()` which constructs the multi-message array. For each ancestor in the chain: emit the user message (from that turn's hash_ids), then the assistant message (from captured output), then the next user message (from the remaining delta hashes minus the output blocks).

3. **After request completes**: extract `output_text` from the response metrics and call `tos.complete(data_index, output_text)`, which broadcasts to any waiters.

The output text is captured from standard OpenAI API responses -- `choices[].message.content` for non-streaming, accumulated `choices[].delta.content` for streaming. No engine modifications required.

### What About Token Count Accuracy?

A natural question: does `enc(dec(output_tokens)) == output_tokens`? If the engine produced token IDs `[t1, t2, ..., tn]`, and we decode them to text and re-encode, do we get the same IDs back?

For canonical BPE tokenization: yes. The engine's output tokens were produced by greedy or sampled decoding from the vocabulary. The `decode` step converts them to a string via the tokenizer's merge table. The `encode` step applies the same BPE algorithm deterministically. Since canonical tokens always roundtrip through `encode(decode(x))`, the re-encoded sequence matches.

The template tokens surrounding the assistant message create pre-tokenization barriers that isolate it from adjacent messages. BPE merges cannot cross these barriers, so the re-encoding of the assistant message content is independent of what comes before or after it in the template.

There are edge cases -- non-canonical tokenizations from speculative decoding, cross-UTF-8-boundary byte tokens -- but these are vanishingly rare in practice with Qwen2-family tokenizers. And for engines that support `return_token_ids=true` (vLLM), the roundtrip question is bypassed entirely.

## Validation

### Production Engine: vLLM v0.10.0 + Qwen3-30B-A3B

We ran the same Bailian multi-turn trace against vLLM with automatic prefix caching enabled, comparing Content mode (baseline) vs Messages mode (`--track-output`):

| Mode | Prefix Cache Hit Rate |
|------|----------------------|
| Content (baseline) | 26.7% |
| Messages (`--track-output`) | 38.4% |

A **+11.7 percentage point** improvement from accurately modeling the multi-turn conversation structure.

### Python Simulator

A standalone Python simulator (`scripts/simulate_cache.py`) models the block-level prefix cache with LRU eviction, confirming the mechanism:

| Mode | Simulated Hit Rate |
|------|-------------------|
| Content | 28.6% |
| Messages | 42.9% |

The simulator numbers are slightly higher because they model an idealized cache without fragmentation or scheduling effects. The directional improvement matches.

## Formal Properties

Three invariants ensure correctness. We state them here; the full proofs are in the companion paper.

**Length constraint.** `|enc(inflate_text)| == input_length` is preserved by BOS/EOS framing. Each block generated by `TokenSampler` is framed with `<|im_start|>` and `<|im_end|>`, which are added tokens that create deterministic pre-tokenization boundaries. The framing guarantees that `enc` distributes over block concatenation, and the generator loop retries until the total token count matches exactly.

**Block congruence.** Same `hash_id` produces the same text produces the same tokens. This is maintained through all three transformations in the pipeline -- `inflate_hashes()` memoizes in `user_prompts`, `message()` constructs deterministic JSON, and `template()` is a fixed function of the message array. Determinism of all three functions preserves congruence end-to-end.

**Output matching.** `enc(dec(output_tokens)) == output_tokens` for canonical BPE, because template tokens create pre-tokenization barriers around the assistant message. The `<|im_end|>` after the assistant content and `<|im_start|>` before the next user content isolate the output text from adjacent content during re-tokenization. Within the assistant message boundary, BPE `encode` is deterministic and canonical tokens are roundtrip-stable.

## Takeaway

If your benchmark tool generates synthetic prompts for multi-turn conversations, the question isn't whether prefix caching works -- it's whether your tool is constructing prompts in a way that *allows* prefix caching to work. The template suffix barrier means that stuffing everything into a single message will silently defeat prefix matching at every turn boundary, no matter how carefully you match the content.

The fix is structural: use the actual multi-turn message format that the chat template was designed for, inject real output text into the assistant role, and let the template tokens do what they were always meant to do -- serve as alignment barriers between turns.

`--track-output` is available in request-sim for the `bailian` dataset in `trace-replay` mode. It requires no engine modifications and works with any OpenAI-compatible serving backend.
